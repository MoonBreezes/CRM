[tox]
minversion = 3.8.0
envlist = py311
isolated_build = true

[gh-actions]
python =
    3.11: py311

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements.txt
commands =
    python -m unittest discover -s test -p '*_test.py'


